// program to compute floor valuse and ceiling value
#include<stdio.h>
#include<math.h>
int main()
{
	int f,p;
	float a;
	printf("Enter a decimal value: ");
	scanf("%d",&a);
    c=ceil(a);
	f=floor(a);
	printf("The floor value is %d and ceiling value is %d",f,c);
}
